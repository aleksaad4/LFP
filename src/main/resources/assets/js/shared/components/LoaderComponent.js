const LoaderComponent = {
    templateUrl: "/pages/shared/components/loader.html",
    bindings: {
        isLoading: "<"
    },
    controller: function () {
    }
};

export default LoaderComponent;